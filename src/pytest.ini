[pytest]
# The web3-provided "pytest_ethereum" plugin fails to import under the pinned
# eth-typing version and is unrelated to this project's tests. Disable it so
# the suite collects cleanly in CI.
addopts = -p no:pytest_ethereum
testpaths = tests
